spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:
        - id: client-service
          uri: http://localhost:8082
          predicates:
            - Path=/client/**
          filters:
            - AuthenticationFilter

server:
  port: 8081
